{
  "version": 2,
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "outputDirectory": "dist"
  },
  "routes": [
    { "handle": "filesystem" },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}